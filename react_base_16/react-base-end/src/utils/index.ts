// 数组的深层

export const deepthArr = function(arr:[any]):[any]{
  for (let index = 0; index < arr.length; index++) {
    const element = arr[index];
    if(typeof element!=='object'||element===null){
      
    }
  }
}